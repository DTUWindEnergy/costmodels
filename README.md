# CostModels

Development install (requires `pixi` https://pixi.sh/latest/#installation):

```sh
pixi shell -e test # env activate 
pre-commit install # hooks (run only once after install)
pytest # run tests
```
